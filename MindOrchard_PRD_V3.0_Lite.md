# 思想果园 (MindOrchard) —— AI 时代个人知识库与博客系统

> **文档版本**：V3.0 Lite  
> **基于**：MindOrchard PRD V2.0 + AI_Knowledge_Blog_PRD 融合精简版  
> **核心命题**：以 MindOrchard 的深度架构与 UX 理念为蓝图，采用 AI_Knowledge_Blog 的轻量技术栈，打造可快速落地的个人知识操作系统。

---

## 第一部分：问题定义与设计哲学

### 1.1 起点：AI 时代，博客的价值何在？

随着 LLM 能够生成高质量文字，静态博客的"信息传递"功能已被 AI 替代。但人类博客不会消失——它进化为：

- **信任锚点**：AI 可以写文章，但人类的审美排版、个人文风、持续输出的思想轨迹是**不可伪造的签名**。
- **思维外脑**：博客不再是发表即结束的公告板，而是**持续生长的私人知识图谱**。
- **人机共读**：同一份内容既要让人类流畅阅读，又要让 AI 精确理解与推理。

### 1.2 核心矛盾与架构定调

| 维度 | 人类需求 | AI 需求 |
| :--- | :--- | :--- |
| 内容形态 | 完整的文章、流畅的排版 | 原子化的语义片段 |
| 交互方式 | 无弹窗、无打扰的心流 | 结构化、可检索、可关联 |
| 权限感知 | 直觉式的公开/私密切换 | 精确的位级权限控制 |

**解决方案**：底层以 **Atom（思想原子）** 为最小存储单元，UI 层**绝对隐藏**原子复杂性。人类感知到的是完整文章，AI 消费的是可拆解的语义结构。

### 1.3 设计原则

- **Human Flow First**：写作心流不可打断
- **Structure Hidden, Not Absent**：结构存在但对用户透明
- **AI as Consumer**：AI 消费结构但不控制创作
- **Article is Source of Truth**：文章为唯一真相源，Atom 为派生结构
- **Progressive Enhancement**：能力渐进增强，任何降级不阻塞主流程

---

## 第二部分：系统总体架构

### 2.1 三层架构

```
┌─────────────────────────────────────────┐
│        Human Experience Layer           │
│   写作编辑器 · 阅读界面 · 发布管理      │
├─────────────────────────────────────────┤
│        Knowledge Structuring Layer      │
│   Atom 提取 · Embedding · 知识图谱      │
│   版本控制 · 渗透压权限 · 事件溯源      │
├─────────────────────────────────────────┤
│        AI Intelligence Layer            │
│   RAG · Agent · 语义检索 · 文风校准     │
└─────────────────────────────────────────┘
```

### 2.2 技术栈选型（轻量务实）

| 层级 | 组件 | 选型 | 替代说明 |
| :--- | :--- | :--- | :--- |
| 主数据库 | MongoDB | ✅ 文档存储 + 原子级灵活 schema | — |
| 搜索引擎 | Elasticsearch | ✅ 全文搜索 + 聚合分析 | — |
| 向量检索 | MongoDB Atlas Vector Search | ✅ 替代 pgvector/Qdrant，无需额外组件 | 轻量替代 |
| 消息队列 | Kafka | ✅ Atom 生成/Embedding 异步 pipeline | — |
| 任务队列 | BullMQ (Redis) | ✅ 异步 AI 任务调度 | 保留，轻量实用 |
| 离线计算 | MongoDB Aggregation + Cron | ✅ 替代 Apache Spark | 轻量替代 |
| 工作流 | Cron Job + BullMQ | ✅ 替代 Temporal | 轻量替代 |
| CRDT | Yjs | ✅ 本地优先离线编辑 | 保留，解决真问题 |
| 文风校准 | Prompt + Few-shot | ✅ 替代 LoRA 微调 | 轻量替代 |
| 可观测性 | 应用日志 + 错误追踪 | ✅ 替代 OpenTelemetry | 轻量替代 |
| 前端缓存 | IndexedDB + Service Worker | ✅ 离线可用 | — |

### 2.3 数据模型

#### Article（文章）
| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `id` | ObjectId | 唯一标识 |
| `title` | string | 标题 |
| `draft_version_id` | ObjectId → ArticleVersion | 草稿版本指针 |
| `published_version_id` | ObjectId → ArticleVersion | 发布版本指针 |
| `tags` | string[] | 标签 |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

#### ArticleVersion（文章版本）
| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `id` | ObjectId | 唯一标识 |
| `article_id` | ObjectId → Article | 所属文章 |
| `atom_sequence` | ObjectId[] → Atom | Atom 有序序列 |
| `created_at` | datetime | 版本时间 |
| `created_by` | string | 作者标识 |

#### Atom（思想原子）
| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `id` | ObjectId | 唯一标识 |
| `content_hash` | string | 内容哈希，去重用 |
| `raw_text` | string | 原始文本内容 |
| `visibility_mask` | int | 位掩码权限（见 2.4） |
| `embedding` | vector | 语义向量（MongoDB Atlas Vector） |
| `cluster_id` | string | 语义聚类 ID |
| `superseded_by_id` | ObjectId → Atom (self) | 被哪个 Atom 替代 |
| `reference_count` | int | 被引用次数 |
| `created_at` | datetime | 创建时间 |
| `is_evergreen` | bool | 是否常青内容（不衰减） |

#### Relation（关联关系）
| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `from_atom_id` | ObjectId → Atom | 源 Atom |
| `to_atom_id` | ObjectId → Atom | 目标 Atom |
| `relation_type` | enum | `same_topic` / `contradicts` / `extends` / `references` |
| `strength` | float | 关联强度 (0.0–1.0) |

#### AtomEvent（原子事件 · 事件溯源）
| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `id` | ObjectId | 唯一标识 |
| `atom_id` | ObjectId → Atom | 关联 Atom |
| `event_type` | enum | `created` / `edited` / `superseded` / `deleted` |
| `payload` | JSON | 事件载荷（旧值/新值/元数据） |
| `created_at` | datetime | 事件时间 |
| `created_by` | string | 触发者 |

### 2.4 渗透压权限模型（位掩码）

`visibility_mask` 字段 (Int)：

| 位 | 值 | 含义 |
| :--- | :--- | :--- |
| Bit 0 | `0b001` = 1 | 人类可见（公开） |
| Bit 1 | `0b010` = 2 | AI 可见（训练/检索） |
| Bit 2 | `0b100` = 4 | 仅作者可见（私密） |

组合示例：
- `0b001` (1) = 仅公开可见
- `0b010` (2) = 仅 AI 可见（私密笔记可用于检索但不可阅读）
- `0b110` (6) = AI 可见 + 人类可见
- `0b100` (4) = 完全私密

检索时强制注入权限过滤：
```
// AI 检索：mask & 0b010 > 0
// 人类公开阅读：mask & 0b001 > 0
// 作者本人：mask & 0b111 > 0（全量可见）
```

---

## 第三部分：核心机制

### 3.1 Atom 生命周期

```
Author writes Article
        │
        ▼
  ┌──────────────┐
  │  CRDT Editor │  (Yjs, 本地 IndexedDB)
  └──────┬───────┘
         │ 段落保存
         ▼
  ┌──────────────┐     ┌──────────────┐
  │ Atom Events  │────▶│    Atom      │
  │ (append only)│     │ (projected)  │
  └──────────────┘     └──────┬───────┘
                              │
                    ┌─────────┼─────────┐
                    ▼         ▼         ▼
              ┌────────┐ ┌────────┐ ┌────────┐
              │Embedding│ │ Graph  │ │Version │
              │(向量索引)│ │(关联关系)│ │(快照)  │
              └────────┘ └────────┘ └────────┘
```

### 3.2 事件溯源与一致性

- **不执行物理 `UPDATE` 或 `DELETE`**：所有变更作为不可变事件追加到 `AtomEvent` 表
- **软删除**：删除段落仅记录 `deleted` 事件，Atom 本身保留，进入"碎片箱"
- **当前状态投影**：从事件流中投影计算出当前 Atom 状态
- **发布 = 指针交换**：发布仅更新 Article 的 `published_version_id` 指针，瞬间生效且无拷贝开销

### 3.3 版本检索三阶过滤

当用户搜索或 AI 检索时：

1. **状态准入**：只检索 `active` 且未被替代的 Atom（`superseded_by_id IS NULL`）
2. **同源聚类抑制**：同一观点的新旧版本，只展示最新活跃版
3. **时间衰减权重**：`Score = Base_Score * e^(-λ * Δt)`，技术文章 λ=0.1（衰减快），散文/诗歌 λ=0.001（几乎不衰减）；`is_evergreen = true` 锁定权重为 1.0

### 3.4 冲突处理

- **段落级 Last Write Wins**：多设备同时编辑同一段落，后保存者覆盖
- **非阻塞通知**：被覆盖端编辑器顶部浮现无声提示："段落 X 已被其他设备更新"
- **版本可回滚**：所有历史版本通过 ArticleVersion 可恢复

---

## 第四部分：分阶段产品需求

> **全局架构约束（贯穿所有阶段）**：
> 1. **不可变原则**：`Atom` 一旦写入，永不物理覆写或删除
> 2. **本地优先 (Local-First)**：编辑器基于 Yjs CRDT，断网编辑无损
> 3. **动静分离**：静态资源 CDN，动态 API 边缘计算
> 4. **优雅降级**：AI 服务超时不报错，任何错误不阻塞主流程

---

### 阶段一：稳固的基石 —— 完美的创作容器

**战略目标**：建立让人类作者产生依赖的写作环境。底层确立 Atom 存储模型与事件溯源，UI 层**绝对隐藏**原子复杂性。

**优先级**：P0 核心 | **预计周期**：4–6 周

#### 产品与 UX 设计

| 编号 | 功能模块 | 体验细节 |
| :--- | :--- | :--- |
| **P1-UX-01** | **禅意块编辑器** | 非当前编辑段落自然降低透明度。光标所在段落有极细的呼吸感金色描边（聚焦环）。按下回车时，上一段有 0.2s 弹性缓动，仿佛新段落从旧段落"生长"出来。 |
| **P1-UX-02** | **智能 `/` 命令** | 输入 `/` 时菜单根据上文动态排序。若上文在列举数据，"插入图表"排首位；若为英文，"翻译/改写"置顶。 |
| **P1-UX-03** | **段落拖拽重排** | 按住段落左侧拖拽图标，段落生成半透明跟随效果。松手时融入新位置，带有物理弹性动画。 |
| **P1-UX-04** | **无感化保存** | 编辑器右上角抽象圆环指示器。未保存时为哑光灰；同步至云端成功瞬间闪过顺时针极光绿。无文字提示，不打断心流。 |
| **P1-UX-05** | **版本时间轴** | 编辑器右侧边缘有隐约的时间刻度尺。鼠标靠近时刻度尺亮起，上下滚动即可在历史版本中实时预览，松手回弹至当前版本。 |
| **P1-UX-06** | **版本差异可视化** | 浏览历史版本时，新增处泛起极淡樱花粉，删除处显示雾霾蓝。人类扫一眼便知改动位置。 |
| **P1-UX-07** | **渗透压指示器 V1** | 三段式滑块：🔒私密 / 🤖AI可见 / 🌐公开。从私密滑向公开时有物理阻尼感并弹出问候："已经准备好让别人看见这些想法了吗？" |
| **P1-UX-08** | **文章卡片流** | 每篇文章显示为带有模糊渐变底纹的卡片，底纹颜色由 AI 分析标题情绪生成。支持触控板手势：双指右滑归档，左滑置顶。 |

#### 架构与工程

| 编号 | 模块 | 设计细节 |
| :--- | :--- | :--- |
| **P1-ARC-01** | **Atom 存储引擎** | 事件溯源模式。所有编辑操作作为不可变事件写入 `AtomEvent` 集合。当前状态由事件流投影计算得出。绝不执行物理 `UPDATE` 或 `DELETE`。 |
| **P1-ARC-02** | **前端 CRDT** | 使用 **Yjs** 作为编辑器底层数据结构，本地 IndexedDB 为 Source of Truth。网络层用于后台静默同步。断网编辑无损。 |
| **P1-ARC-03** | **文章-原子映射** | `Article` → `ArticleVersion`（含 atom_sequence 数组） → `Atom`。一个 Atom 可被多篇文章、多个版本安全引用。 |
| **P1-ARC-04** | **草稿与发布分离** | 访客 API 只读取 `published_version_id`；作者编辑器读取 `draft_version_id`。发布仅是一次指针的原子交换。 |
| **P1-ARC-05** | **编辑冲突** | 段落级 Last Write Wins + 非阻塞通知。被覆盖端显示无声黄色提示条。 |

---

### 阶段二：觉醒 —— 私有知识的"场"效应

**战略目标**：引入 AI 语义检索，让私有笔记形成隐形知识磁场。写作时能被动感知到相关记忆，无需主动搜索。

**优先级**：P1 增强 | **预计周期**：3–4 周

#### 产品与 UX 设计

| 编号 | 功能模块 | 体验细节 |
| :--- | :--- | :--- |
| **P2-UX-01** | **沉思侧边栏** | 光标在某段落停留 4 秒无输入，屏幕最右侧泛起 1px 宽呼吸紫罗兰光晕。鼠标靠近光晕，侧边栏丝滑拉出，展示 AI 关联笔记。鼠标移开 3 秒后自动收起。非模态、不阻塞。 |
| **P2-UX-02** | **关联笔记卡片** | 卡片上不仅显示内容，还高亮一句 AI 注解："这篇日记里你提到了同样的'失控感'。" 心智模型补全，而非关键词匹配。 |
| **P2-UX-03** | **碎片箱** | 被删除且无引用的 Atom 显示为"思绪堆肥箱"。鼠标悬停时微微展开显示前几个字。拖拽到编辑器时带有吸入动画。 |
| **P2-UX-04** | **AI 摘要生成** | 摘要框内文字像墨水洇入宣纸一样逐字显现，让用户感知"AI 正在书写"。可重写切换语气。 |
| **P2-UX-05** | **段落级渗透压** | 聚焦环左侧的微型水滴图标菜单：💧灰色=私密 / 💧半透紫=AI可见 / 💧金色=公开。Atom 概念在 UI 层的唯一可视化泄漏。 |
| **P2-UX-06** | **AI 文风格调** | 三个推子：🍷严肃理性 / 🧂幽默犀利 / ☕️温柔治愈。调好"口味"后实时生成改写预览。基于 Prompt + Few-shot 实现，无需模型微调。 |

#### 架构与工程

| 编号 | 模块 | 设计细节 |
| :--- | :--- | :--- |
| **P2-ARC-01** | **向量检索** | 使用 **MongoDB Atlas Vector Search**。检索时强制 Pre-filter：`{ visibility_mask: { $bitsAllSet: [1] } }`（AI 可见位），再进行向量相似度计算。严禁裸搜向量库。 |
| **P2-ARC-02** | **渗透压过滤** | 所有查询管道强制注入 `visibility_mask` 条件。应用层封装统一中间件，确保无遗漏。 |
| **P2-ARC-03** | **碎片箱自动化** | `AtomJanitor` Cron 任务每日凌晨扫描 `reference_count = 0` 的 Atom，不物理删除，仅标记进入碎片箱命名空间。 |
| **P2-ARC-04** | **AI 异步队列** | 摘要生成、关联推荐等 AI 任务入 **BullMQ** 队列。超时或失败时前端静默展示温柔文案，绝不阻断保存或发布主流程。 |
| **P2-ARC-05** | **语义索引构建** | 新 Atom 保存后通过 Kafka pipeline 异步生成 embedding，30 秒内进入检索范围。每周日凌晨全量重建索引保证一致性（MongoDB Aggregation 替代 Spark）。 |

---

### 阶段三：绽放 —— 时间维度的思想图谱

**战略目标**：利用 Atom 历史数据，为作者和访客创造"看见思维如何生长"的体验。

**优先级**：P2 深度 | **预计周期**：4–5 周

#### 产品与 UX 设计

| 编号 | 功能模块 | 体验细节 |
| :--- | :--- | :--- |
| **P3-UX-01** | **个人思想星图** | 节点为文章/核心 Atom，连线为流动光粒子贝塞尔曲线。语义关联越强，光粒子流速越快。按住 Space 键，光标变为放大镜，周围节点被引力吸附聚集。 |
| **P3-UX-02** | **思维演化纪录片** | 点击"播放我的 2026"，系统自动挑选关键 Atom 生成 60 秒自动运镜视频，展示思维转变轨迹。 |
| **P3-UX-03** | **文章演化地层** | 文章底部展示水平地层剖面纹理，每一层代表一个历史版本。访客横向涂抹可刮开表层露出旧版本毛边摘要。阅读变成轻量级思想考古。 |
| **P3-UX-04** | **AI 分身问答** | 访客"问问作者"后，回答以撕纸便签形式贴在文章旁边。引用作者原文原句并附 AI 转述。可拖动、可收纳。 |
| **P3-UX-05** | **隐私诗学反馈** | 访客问题涉及私密内容时，便签显示隐喻："关于这件事，作者曾在花园深处埋下过一颗种子，但尚未准备好让它发芽。" 拒绝冷冰冰的"权限不足"。 |

#### 架构与工程

| 编号 | 模块 | 设计细节 |
| :--- | :--- | :--- |
| **P3-ARC-01** | **演化链数据结构** | `Atom` 表 `superseded_by_id` 自引用字段形成替代链表。通过 MongoDB `$graphLookup` 查询链头，废弃版本自动排除出常规检索。替代 Neo4j。 |
| **P3-ARC-02** | **知识图谱计算** | MongoDB Aggregation Pipeline + 定时 Cron 任务，每周计算一次全局 Atom 关联度，生成 `similarity_score` 存入 `Relation` 集合。替代 Apache Spark。 |
| **P3-ARC-03** | **访客问答 RAG 安全层** | 请求经过权限中间件：(1) 全局限流 (2) 强制注入 `visibility_mask & 0b001 > 0` 过滤 (3) 返回前 PII 脱敏。 |
| **P3-ARC-04** | **文风校准** | **Prompt + Few-shot 架构**：为每位作者保存 5–10 篇代表性文章作为 Few-shot 示例，推理时动态注入系统 Prompt。替代 LoRA 微调。支持作者手动上传"风格参考文本"。 |
| **P3-ARC-05** | **地层数据 API** | `/api/article/{id}/strata` 返回文章各历史版本的摘要、时间戳、替代关系。前端虚拟滚动优化长列表。 |

---

### 阶段四：自治 —— 知识库的"植物神经系统"

**战略目标**：系统具备自我维护、自我报告的能力。作者只在需要时接收优雅的洞察。

**优先级**：P3 运维 | **预计周期**：2–3 周

#### 产品与 UX 设计

| 编号 | 功能模块 | 体验细节 |
| :--- | :--- | :--- |
| **P4-UX-01** | **思想果园周报** | 每周一早上 8 点收到 HTML 邮件。园艺隐喻："上周有 3 片叶子落在了堆肥箱"、"两株植物似乎在争夺阳光（观点矛盾）"。植物标本手帐风，附带柔和插画。 |
| **P4-UX-02** | **后台静默通知** | 默认无任何红点或数字角标。仅当发现重大矛盾或孤立碎片过多时，左侧菜单栏浮现呼吸的琥珀色宝石图标。点击展开洞察报告。 |
| **P4-UX-03** | **矛盾消解向导** | 点击周报中的矛盾链接，打开左旧右新的分屏界面。用户可分别修改，或点击"弥合分歧"由 AI 生成融合草稿。 |
| **P4-UX-04** | **健康仪表盘** | 无数据即无界面。仅当有需要关注的事项时，出现平静的橙色脉动光点。点击展开任务清单。 |

#### 架构与工程

| 编号 | 模块 | 设计细节 |
| :--- | :--- | :--- |
| **P4-ARC-01** | **时间衰减引擎** | 应用层 `computeDecay(timestamp, category)` 函数。λ 根据类型动态调整：技术文章 λ=0.1，散文/诗歌 λ=0.001。`is_evergreen = true` 锁定权重。 |
| **P4-ARC-02** | **矛盾检测** | 每晚 Cron 任务拉取同一 `cluster_id` 下不同版本的 Atom 对，发送至大模型进行矛盾分类（修正/补充/对立）。结果存储 `contradiction_score`，不做自动修改。 |
| **P4-ARC-03** | **数据安全** | `AtomJanitor` 执行清理前记录操作日志，所有变更在 MongoDB 事务中执行，确保可追溯与回滚。 |
| **P4-ARC-04** | **可观测性** | 应用日志 + 结构化错误追踪。关键操作（发布、AI 生成、检索）记录 trace_id。异常时报警但不向用户展示技术细节。 |

---

## 第五部分：架构简化决策记录

| 原 MindOrchard 组件 | 决策 | 理由 | 替代方案 |
| :--- | :--- | :--- | :--- |
| **pgvector / Qdrant** | REPLACE | 个人知识库向量规模 <100 万，无需独立向量数据库 | MongoDB Atlas Vector Search |
| **Neo4j** | REMOVE | 图关系简单，Relation 集合 + `$graphLookup` 足够 | MongoDB 原生图查询 |
| **Apache Spark** | REMOVE | 个人级数据量，单机 Aggregation Pipeline 足够 | MongoDB Aggregation + Cron |
| **Temporal** | REMOVE | 无需复杂分布式工作流，Cron + BullMQ 足够 | BullMQ + Cron Job |
| **LoRA 微调** | REMOVE | 个人级文风格校，Prompt + Few-shot 效果足够 | 系统 Prompt + 风格样本 |
| **BullMQ** | KEEP | 轻量 Redis 队列，完美匹配异步 AI 任务场景 | — |
| **Yjs CRDT** | KEEP | 解决真实离线编辑问题，npm 包轻量 | — |
| **OpenTelemetry** | REMOVE | MVP 阶段过度，应用日志 + 错误追踪足够 | 结构化日志 + Sentry |
| **Kafka** | KEEP | Atom/Embedding pipeline 需要可靠消息传递 | — |
| **六边形架构** | SIMPLIFY | 保留核心领域与外部依赖分离思想，但不抽象为独立包 | 清晰的模块边界 |

---

## 第六部分：阶段路线图与依赖关系

```
Phase 1 (4-6周)          Phase 2 (3-4周)          Phase 3 (4-5周)          Phase 4 (2-3周)
┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ 禅意编辑器    │───▶│ 沉思侧边栏    │───▶│ 思想星图      │───▶│ 周报系统      │
│ Atom 存储    │    │ 向量检索      │    │ 演化地层      │    │ 矛盾检测      │
│ 事件溯源      │    │ 碎片箱        │    │ AI 分身问答   │    │ 静默通知      │
│ 渗透压 V1    │    │ AI 摘要       │    │ 文风格校      │    │ 健康仪表盘    │
│ CRDT 离线    │    │ 渗透压 V2     │    │ 关系图谱      │    │ 衰减引擎      │
│ 版本管理      │    │ 语义索引      │    │ RAG 安全层    │    │ 自动化运维    │
└──────────────┘    └──────────────┘    └──────────────┘    └──────────────┘
     P0                  P1                  P2                  P3
   必须完成             依赖 Phase 1          依赖 Phase 2          依赖 Phase 3
```

**总预计周期**：13–18 周（3–4.5 个月）完成从创作容器到自治系统的全链路。

---

## 第七部分：核心结论

### 系统本质

> 人类写作入口 + AI 推理能力 + 知识结构化 = 认知操作系统

### 一句话总结

> 让人类像写博客一样表达，让 AI 像理解数据库一样理解知识。

### 精简后的核心保留

MindOrchard 的精华得以保留：问题推导逻辑、Atom 架构思想、渗透压权限模型、事件溯源模式、编号系统、UX 设计理念。同时通过替换重量级组件，使整个系统的技术栈回归"个人级"应有的轻量与务实。
